diff --git a/dist/browser-module.mjs b/dist/browser-module.mjs
index 4d0a389de8a3db767f1fef99ed186790431aa844..f24cfbb550a50e09f684f1b6855f9b0d81941eb2 100644
--- a/dist/browser-module.mjs
+++ b/dist/browser-module.mjs
@@ -10878,8 +10878,12 @@ class $69aac16029968692$export$2e2bcd8739ae039 extends (0, $f92906be28e61769$exp
         // We need to decode the glyph if variation processing is requested,
         // so it's easier just to recompute the path's cbox after decoding.
         if (this._font._variationProcessor && !internal) return this.path.cbox;
+        let glyfPos = this._font.loca.offsets[this.id];
+        let nextPos = this._font.loca.offsets[this.id + 1];
+        // Empty glyph (e.g. space) has no outline data — return zero bbox
+        if (glyfPos === nextPos) return Object.freeze(new (0, $f34600ab9d7f70d8$export$2e2bcd8739ae039)(0, 0, 0, 0));
         let stream = this._font._getTableStream('glyf');
-        stream.pos += this._font.loca.offsets[this.id];
+        stream.pos += glyfPos;
         let glyph = $69aac16029968692$var$GlyfHeader.decode(stream);
         let cbox = new (0, $f34600ab9d7f70d8$export$2e2bcd8739ae039)(glyph.xMin, glyph.yMin, glyph.xMax, glyph.yMax);
         return Object.freeze(cbox);
diff --git a/dist/browser.cjs b/dist/browser.cjs
index a5fa901b5608575f84390e3ef0474a7789e8269a..e30f49ccb7d59db869945968411d5b96f692ade7 100644
--- a/dist/browser.cjs
+++ b/dist/browser.cjs
@@ -10895,8 +10895,12 @@ class $f680320fa07ef53d$export$2e2bcd8739ae039 extends (0, $0e4f52d7996e478b$exp
         // We need to decode the glyph if variation processing is requested,
         // so it's easier just to recompute the path's cbox after decoding.
         if (this._font._variationProcessor && !internal) return this.path.cbox;
+        let glyfPos = this._font.loca.offsets[this.id];
+        let nextPos = this._font.loca.offsets[this.id + 1];
+        // Empty glyph (e.g. space) has no outline data — return zero bbox
+        if (glyfPos === nextPos) return Object.freeze(new (0, $0e2da1c4ce69e8ad$export$2e2bcd8739ae039)(0, 0, 0, 0));
         let stream = this._font._getTableStream('glyf');
-        stream.pos += this._font.loca.offsets[this.id];
+        stream.pos += glyfPos;
         let glyph = $f680320fa07ef53d$var$GlyfHeader.decode(stream);
         let cbox = new (0, $0e2da1c4ce69e8ad$export$2e2bcd8739ae039)(glyph.xMin, glyph.yMin, glyph.xMax, glyph.yMax);
         return Object.freeze(cbox);
